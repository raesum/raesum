info:
  name: Get Organization by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/organization/get/1"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
