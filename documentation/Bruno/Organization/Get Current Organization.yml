info:
  name: Get Current Organization
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v1/organization/get/"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
