info:
  name: Get Metadata Keys
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/v1/organization/metadata/get/keys/"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
