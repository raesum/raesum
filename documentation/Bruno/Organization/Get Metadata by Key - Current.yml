info:
  name: Get Metadata by Key - Current
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/v1/organization/metadata/get/byKey/description"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
