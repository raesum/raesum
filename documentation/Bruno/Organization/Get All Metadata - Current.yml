info:
  name: Get All Metadata - Current
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/api/v1/organization/metadata/get/"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
