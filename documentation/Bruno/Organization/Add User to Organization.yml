info:
  name: Add User to Organization
  type: http
  seq: 15

http:
  method: POST
  url: "{{host}}/api/v1/organization/addUserToOrg/"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "value": "test@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
