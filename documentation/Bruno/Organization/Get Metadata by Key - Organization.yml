info:
  name: Get Metadata by Key - Organization
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/v1/organization/metadata/get/byKey/description/1"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
