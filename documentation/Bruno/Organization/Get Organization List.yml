info:
  name: Get Organization List
  type: http
  seq: 15

http:
  method: GET
  url: "{{host}}/api/v1/organization/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
