info:
  name: Get All Metadata - Organization
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/api/v1/organization/metadata/get/1"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
