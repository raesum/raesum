info:
  name: Get File Data by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/v1/file/data/{{fileId}}"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
