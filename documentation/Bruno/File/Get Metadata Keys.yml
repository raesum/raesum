info:
  name: Get Metadata Keys
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/v1/file/metadata/get/keys/"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
