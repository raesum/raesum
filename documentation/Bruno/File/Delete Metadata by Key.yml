info:
  name: Delete Metadata by Key
  type: http
  seq: 9

http:
  method: POST
  url: "{{host}}/api/v1/file/metadata/delete/byKey/{{key}}/{{fileId}}"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
