info:
  name: Get All Metadata
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/api/v1/file/metadata/get/{{fileId}}"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
