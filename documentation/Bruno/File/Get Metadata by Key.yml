info:
  name: Get Metadata by Key
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/v1/file/metadata/get/byKey/{{key}}/{{fileId}}"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
