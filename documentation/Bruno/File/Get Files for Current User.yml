info:
  name: Get Files for Current User
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v1/file/get/byUser/"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
