info:
  name: Get Files by User ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v1/file/get/byUser/{{userId}}"
  headers:
    - name: Origin
      value: "{{host}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
