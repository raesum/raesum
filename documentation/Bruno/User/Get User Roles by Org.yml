info:
  name: Get User Roles by Org
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/v1/auth/role/user/get/1"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
