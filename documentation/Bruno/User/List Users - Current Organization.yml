info:
  name: List Users - Current Organization
  type: http
  seq: 11

http:
  method: GET
  url: "{{host}}/api/v1/user/list/"
  headers:
    - name: Origin
      value: "{{host}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
