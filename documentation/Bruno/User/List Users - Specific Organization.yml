info:
  name: List Users - Specific Organization
  type: http
  seq: 12

http:
  method: GET
  url: "{{host}}/api/v1/user/list/1"
  headers:
    - name: Origin
      value: "{{host}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
