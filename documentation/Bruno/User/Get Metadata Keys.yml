info:
  name: Get Metadata Keys
  type: http
  seq: 13

http:
  method: GET
  url: "{{host}}/api/v1/user/metadata/get/keys/"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
