info:
  name: Get Current Allowed Organizations
  type: http
  seq: 20

http:
  method: GET
  url: "{{host}}/api/v1/user/organization/getAllowed"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
