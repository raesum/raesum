info:
  name: Remove User Roles
  type: http
  seq: 9

http:
  method: POST
  url: "{{host}}/api/v1/auth/role/user/remove/"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "values": [1, 2, "admin"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
