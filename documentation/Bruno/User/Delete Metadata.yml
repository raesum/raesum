info:
  name: Delete Metadata
  type: http
  seq: 23

http:
  method: POST
  url: "{{host}}/api/v1/user/metadata/delete/byKey/family_name/1"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
