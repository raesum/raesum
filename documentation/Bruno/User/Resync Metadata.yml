info:
  name: Resync Metadata
  type: http
  seq: 18

http:
  method: GET
  url: "{{host}}/api/v1/user/metadata/resync"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
