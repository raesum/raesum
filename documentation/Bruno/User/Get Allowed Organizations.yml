info:
  name: Get Allowed Organizations
  type: http
  seq: 19

http:
  method: GET
  url: "{{host}}/api/v1/user/organization/getAllowed/1"
  headers:
    - name: Origin
      value: "{{host}}"
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
