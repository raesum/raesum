opencollection: 1.0.0

info:
  name: Raesum
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  headers:
    - name: Origin
      value: "{{host}}"
  variables:
    - name: host
      value: https://curbconnection.raesum.com
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
    presets:
      request:
        type: http
