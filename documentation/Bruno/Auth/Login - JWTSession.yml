info:
  name: Login - JWTSession
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/api/v1/auth/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
