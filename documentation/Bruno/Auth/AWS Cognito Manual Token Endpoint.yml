info:
  name: AWS Cognito Manual Token Endpoint
  type: http
  seq: 3

http:
  method: POST
  url: https://raesum-dev.auth.us-east-1.amazoncognito.com/oauth2/token
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
